var myArray = ["Boyfriend", "Kevin","Mntambo", 24];
var removedFromMyArray = myArray.shift([2]);

console.log(removedFromMyArray)

// shift() works similar to pop(), except that it only removes the FIRST element of the array.
